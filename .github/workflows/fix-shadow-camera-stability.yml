name: Fix shadow camera stability

on:
  pull_request:
    branches:
      - wind-waker-style-cel-shading

jobs:
  delegated-validation:
    if: false
    runs-on: windows-latest
    steps:
      - run: echo "Patch application moved to a dedicated workflow; build-validation performs compilation."
